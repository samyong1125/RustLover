import { TextEdit, VerticalBox } from "std-widgets.slint";

export component MainWindow inherits Window {
    width: 200px;
    height: 200px;

    VerticalBox {
        TextEdit {
            font-size: 14px;
            text: "asdfText_asdfText";
            enabled: true;
        }
    }
}